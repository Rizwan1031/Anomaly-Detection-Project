# Anomaly-Detection-Project
Unsupervised anomaly detection for industrial inspection using pretrained deep feature comparison (PatchCore-style). Detects product defects (cracks, scratches, deformities) without labeled defect data — trained only on normal images. Built with Flask, PyTorch, and ResNet-18. CV mini project.
